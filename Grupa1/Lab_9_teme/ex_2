#include <stdio.h>
#include<stdlib.h>

int cmmdc(int a, int b) {
   if (b == 0)
      return a;
   return cmmdc(b, a % b);
}

int main() {
   int a, b;
   printf("2 numere: ");
   scanf("%d %d", &a, &b);
   printf("cmmd dintre %d si %d este: %d\n", a, b, cmmdc(a, b));
   return 0;
}
