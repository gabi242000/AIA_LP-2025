#include <algorithm>
#include <iostream>
#include <bits/stdc++.h>
#include <vector>
#include <queue>
#include <fstream>
#include <string.h>

using namespace std;


int main()
{
    char a[100];
    cin.get(a, 100);
    int x,y;
    cin>>x>>y;
    char aux;

    aux = a[x];
    a[x] = a[y];
    a[y] = aux;

    cout<<a;
    return 0;
}
