#include <algorithm>
#include <iostream>
#include <bits/stdc++.h>
#include <vector>

using namespace std;

int main()
{
   int n;
   cin >> n;
   int v[n][n];

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
                v[i][j] = i + j + 1;
           cout << v[i][j] << " ";
        }// elementele din zona inferioara vor fi v[i][j] = v[j][i], daca pun aceasi conditie pentru toate elementele
        cout << endl;
    }
    
    return 0;
}
